import styled from "styled-components";

export const DailyReportPageContainer = styled.div`
  flex: 1;
  display: grid;
  grid-template-columns: 2.2fr 1fr;
  grid-template-rows: 20.5rem minmax(auto, 25rem);
  gap: 1.25rem;
`;

export const empty = styled.div``;
